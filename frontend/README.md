# React Frontend

## Run
```
For the user-dashboard, go ahead and go to the user-app folder
  cd frontend/user-app    
once there go ahead and install the dependecies 
npm install
then when everything is installed go ahead run the next command
npm run dev


open your web browser and enter http://localhost:3000/user
from there its still a blank for the dashboard section.
the appliance section is up running with mock data right now